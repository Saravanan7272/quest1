============================================================
Prompt: 001
============================================================
We need to design and implement a Dual-Path Video Dialogue Locator system capable of locating dialogue occurrences in video content from either spoken audio or burned-in visual text. Please execute the following master implementation plan:

1. System Infrastructure & Acquisition:
   - Implement video downloading and media ingestion using yt-dlp.
   - Inspect container video metadata using OpenCV (duration, FPS, resolution, audio presence).

2. Audio Evidence Path (ASR):
   - Implement speech transcription using faster-whisper with word-level timestamps enabled.
   - Implement ASR text matching combining fuzzy phrase partial ratio (0.70 weight) and token coverage ratio (0.30 weight).
   - Configure search window padding (5.0s) and candidate similarity thresholding (0.50).

3. Visual Text Discovery Path (OCR):
   - Implement frame sampling at coarse frame rates (1.0 FPS).
   - Integrate PaddleOCR / RapidOCR engine with bounding box detection and text recognition.
   - Implement box-level character similarity (0.60 weight) and token coverage (0.40 weight) for visual text matching.

4. Phase 0 Environment Gate & Quality Assurance:
   - Perform hardware inspection (CPU/GPU detection), virtual environment validation (.venv), FFmpeg binary availability check, and isolated model loading smoke tests.
   - Set up standard project structure with configuration files (config.yaml), CLI entry points (run.py), unit tests (tests/), and documentation (README.md, APPROACH.md, prompts.txt).

============================================================
Prompt: 002
============================================================
Can you please test the pipeline end-to-end on a short video sample first? Let's use this YouTube URL:
https://youtu.be/dPTKl5H5ftg

Please verify video acquisition, audio transcription, frame sampling, text detection, and candidate score calculation on this sample to ensure the core pipeline runs cleanly without runtime errors before we test longer clips.

============================================================
Prompt: 003
============================================================
Please expand the system architecture from a single ASR MVP into a production-oriented Dual-Path Retrieval Architecture with independent audio and visual discovery channels:

1. Data Models & Schemas:
   - Define central dataclasses in src/models.py: ModalityScores, TrackedBox, TextEvent, Candidate, SearchStats, ASRQuerySpan, VisualTrackSpan, and EvidenceMetadata.

2. Visual Discovery Pipeline:
   - Implement Visual Change Scout at 1.0 FPS using frame pixel difference thresholding.
   - Implement Periodic Text Detector at 0.5 FPS as recall insurance for static backgrounds.
   - Implement Trigger Merging with a 2.0s temporal merge window.
   - Implement Dense Sampling within triggered windows at 3.0 FPS.
   - Implement Multi-Box IoU Tracking linking text bounding boxes across consecutive frames into continuous tracks.

3. Candidate Association & Score Fusion:
   - Implement score fusion with missing-modality normalization in src/scoring.py (ASR weight: 0.30, OCR weight: 0.50, Semantic weight: 0.20).
   - Implement candidate temporal association merging audio and visual candidates within association windows.
   - Implement top-K candidate deduplication returning structured JSON output contracts.

============================================================
Prompt: 004
============================================================
Please apply the following precision refinements and test suite expansions:

1. Strict OCR Engine API Boundary Separation:
   - Separate text detection from full OCR recognition: use dedicated detection-only models for candidate box discovery, and full recognition models only for cropped text regions.

2. Terminology & Limitations:
   - Formalize dual-path retrieval terminology across codebase and documentation (Audio Evidence Path vs. Visual Discovery Path).
   - Explicitly document temporal recall limitations for extremely short-lived visual text (< 1.0s duration).

3. Unit Test Suite Expansion:
   - Add dedicated unit test cases for static scene text (periodic detector check), simultaneous multi-box tracking, and text disappearance/reappearance across frames.
   - Expand unit test coverage across all test modules in tests/.

============================================================
Prompt: 005
============================================================
Do not declare the system fully verified until we perform deep root-cause debugging across the visual pipeline and timestamp propagation:

1. Visual Discovery Diagnostics:
   - Investigate why visual discovery currently returns tracked_events: 0 and visual_text_match: false on test clips.
   - Log each visual pipeline stage explicitly: Scout Triggers -> Periodic Triggers -> Merged Windows -> Dense Sampling -> Text Detection -> IoU Tracking -> Frame Selection -> OCR Recognition -> Fuzzy Matching.

2. ASR Candidate Timestamp Propagation:
   - Verify that ASR candidate start timestamps are correctly propagated to candidates rather than falling back to 0.0s.

3. Exact Frame Diagnostic Test:
   - Create a dedicated single-frame diagnostic test verifying text detection bounding boxes, OCR text recognition, and similarity scoring on exact video frames containing on-screen captions.
   - Re-run test suite and E2E validation to confirm BOTH speech_match: true AND visual_text_match: true for multimodal clips.

============================================================
Prompt: 006
============================================================
Please freeze the Golden Evaluation Test Suite and document permanent evaluation benchmark specifications across README.md, APPROACH.md, and tests/fixtures/README.md:

1. Test Media & Queries:
   - Test Video 1 (YVvD7SZ7kc0): Spoken-only dialogue queries ("Why Do We Fall?", "so that we can learn to pick ourselves", "have you quite given up on me?") and Silent Visual Text query ("Thank you for watching").
   - Test Video 2 (WZORRHNP9_w): Multimodal Spoken + Visual query ("At least tell me your name") and Negative Control queries ("Batman", "The blue elephant is dancing").

2. Golden Matrix Definition:
   - Create machine-readable manifest tests/fixtures/golden_tests.yaml defining exact ground truth modalities, expected status (FOUND / NOT_FOUND), expected modality sources (["asr"], ["ocr"], ["asr", "ocr"]), and expected evidence flags.
   - Document temporal recall limitations for short-lived silent visual text (<0.5s duration).

============================================================
Prompt: 007
============================================================
Please execute the complete Golden Evaluation Test Suite end-to-end:

1. Environment & Cleanup:
   - Remove outdated demo references and clean stale temporary test artifacts.
   - Validate tests/fixtures/golden_tests.yaml containing the 7 official test cases.

2. Execution & Output Preservation:
   - Run all 7 test cases individually, outputting actual execution logs and full JSON response contracts.
   - Save individual result JSON files into tests/evaluation_results/case_01.json through case_07.json.
   - Copy evidence JPG images into tests/evaluation_results/evidence/case_01/ through case_05/.
   - Generate human-readable markdown report tests/evaluation_results/GOLDEN_TEST_REPORT.md containing environment metadata and full test matrix results.

============================================================
Prompt: 008
============================================================
Please update .gitignore and output directory structure to version-control golden evaluation evidence:

1. Directory Restructuring:
   - Update .gitignore so outputs/evaluation/ is version-controlled and NOT ignored by Git.
   - Keep temporary runtime files under outputs/runtime/ and outputs/tmp/ ignored.

2. Evidence File Organization:
   - Organize evidence JPG images for golden evaluation cases under outputs/evaluation/case_01/ through case_05/.
   - Verify Git tracking state using git check-ignore -v to confirm evidence images are tracked.

============================================================
Prompt: 009
============================================================
Please execute final golden suite verification and URL sanitization:

1. Input URL Handling:
   - Add automatic URL markdown sanitization stripping bracket wrappers like [...] from incoming video URLs.

2. Suite Execution:
   - Perform clean execution across all 7 Golden Test Cases (Case 1: "Why Do We Fall?", Case 2: "so that we can learn to pick ourselves", Case 3: "Thank you for watching", Case 4: "have you quite given up on me?", Case 5: "At least tell me your name", Case 6: "Batman", Case 7: "The blue elephant is dancing").
   - Save structured output JSON files into tests/evaluation_results/case_01_why_do_we_fall.json through case_07_blue_elephant.json.
   - Save evidence frames under outputs/evaluation/case_01/ through case_05/.
   - Verify multimodal score fusion on Case 5 and negative control precision on Cases 6 & 7 (NOT_FOUND).
   - Publish GOLDEN_TEST_REPORT.md and commit benchmark evidence artifacts.

============================================================
Prompt: 010
============================================================
Halt test suite execution and instrument deep stage-by-stage observability for Case 3 ("Thank you for watching" on YVvD7SZ7kc0):

1. Deep Stage Observability Logging:
   - Log Scout & Periodic triggers: raw_change_triggers, raw_periodic_triggers, merged_trigger_windows (start, end, duration, source), and triggered_duration_ratio.
   - Log Dense Sampling: dense_fps, requested_dense_frames, actual_dense_frames, and duplicate_frames_skipped.
   - Log Text Detection: per-frame timestamp, number_of_boxes, and box_confidences.
   - Log Tracking: track_id, start, end, duration, number_of_boxes, and best_frame_timestamp.
   - Log Unfiltered OCR: per-track track_id, best_frame_timestamp, ocr_text, ocr_confidence, and query_similarity.

2. Diagnostic Evidence & Profiling:
   - Export best frame images for all visual tracks into outputs/debug_case_03/track_000.jpg, track_001.jpg, etc.
   - Export structured track metadata to outputs/debug_case_03/tracks.json.
   - Track stage profiling timers: scout_time, periodic_detection_time, trigger_merge_time, dense_sampling_time, text_detection_time, tracking_time, ocr_time, candidate_matching_time, and total_runtime.

============================================================
Prompt: 011
============================================================
We need to redesign the video text/dialogue localization pipeline to solve temporal timestamp mismatches:

1. Word-Level Timestamp ASR Query Span Extraction:
   - Whisper word timestamps are estimates, not frame-accurate ground truth.
   - Distinguish ASR_ESTIMATED_QUERY_SPAN (asr_query_start, asr_query_end) from VISUAL_EVIDENCE_TIMESTAMP.
   - Implement find_query_span() in src/asr.py to extract exact word-level query bounds from Whisper word-level timestamp lists.

2. ASR-Guided Targeted Visual Search Windowing:
   - When a strong ASR candidate is found, restrict the visual search window to [asr_query_start - 1.5s, asr_query_end + 3.5s] instead of scanning the full video.
   - When ASR candidate is weak or absent (e.g., Case 3 silent visual text), fall back to global visual scout (1.0 FPS) with coarse text-bearing trigger filtering.

============================================================
Prompt: 012
============================================================
APPROVED WITH REQUIRED CHANGES. Please incorporate these three corrections before proceeding:

1. Configurable OCR Acceptance Policy:
   - Implement multi-metric candidate association combining temporal compatibility (5.0s window), normalized token overlap, fuzzy character similarity, OCR confidence, and query similarity thresholding (ocr_min_threshold = 0.45).

2. Representative Track Frame Sampling:
   - Sample candidate frames across the full track duration [track.start, ..., track.end], evaluate OCR query similarity for each frame, and dynamically set best_frame_timestamp to the frame with the highest query similarity.

3. Function Shadowing Bug Fix:
   - Diagnose TypeError: associate_and_fuse_candidates() got an unexpected keyword argument 'ocr_min_threshold'.
   - Remove stale duplicate inline function def associate_and_fuse_candidates from src/pipeline.py (lines 37–93) and import the canonical implementation from src.candidate_association.

============================================================
Prompt: 013
============================================================
STOP architectural changes. Freeze current working behavior as v1.0 and perform complete repository cleanup:

1. Codebase Cleanup:
   - Remove temporary debug files, temporary inspect logs, generated outputs, downloaded media, caches, stale duplicate functions, and development artifacts.
   - Fix final JSON serializer to ensure valid, non-duplicated JSON outputs.

2. Verification & Version Control:
   - Audit codebase for hardcoded credentials, personal tokens, or machine-specific absolute paths.
   - Preserve all 36 unit tests across 11 test modules.
   - Create clean .gitignore and master README.md.
   - Create git commit and tag release v1.0.0.

============================================================
Prompt: 014
============================================================
Perform a strict technical documentation audit and rewrite README.md strictly derived from codebase authority:

1. Repository Source Audit:
   - Audit source code, config files (config.yaml vs config.dev.yaml), test suite, scripts, and verified golden case JSON outputs.
   - Correct all stale parameters (association_window = 5.0s, dense_fps = 3.0, test_count = 36).

2. Modular Engineering Documentation:
   - Create modular technical documentation files under docs/: ARCHITECTURE.md (deep stage breakdown and formulas), CONFIGURATION.md (parameter specification table), TESTING.md (unit tests and golden benchmark matrix), and OUTPUT_SCHEMA.md (JSON response contract).
   - Ensure 100% relative repository links; do NOT use local file:/// scheme links or Windows absolute paths.

============================================================
Prompt: 015
============================================================
Integrate a professional Mermaid architecture diagram system into the documentation:

1. System Architecture Diagram:
   - Create a Level 1 Executive System Architecture flowchart showing Dual-Path Retrieval, Candidate Association & Score Fusion, and Modality-Aware Evidence Selection.

2. Visual Search Funnel Diagram:
   - Create a Level 2 Visual Search Funnel flowchart detailing the Progressive Visual Search Reduction policy using verified Case 3 metrics (121.31s video -> 61 windows -> 6 text-bearing windows -> 36 dense frames -> 11 tracks -> 99 OCR calls -> 111.500s evidence frame).

3. File Integration:
   - Embed GitHub-native Mermaid flowcharts directly into README.md and docs/ARCHITECTURE.md.
   - Create standalone version-controlled .mmd diagram files under docs/diagrams/.

============================================================
Prompt: 016
============================================================
Add explicit reference sections, execution commands, and video links for ALL 7 Golden Test Cases:

1. Benchmark Reference Sections:
   - Add explicit subsections and CLI execution commands for Case 1 through Case 7:
     - Case 1: Spoken Query ("Why Do We Fall?") -> execute_single_case.py 1
     - Case 2: Spoken Query ("so that we can learn to pick ourselves") -> execute_single_case.py 2
     - Case 3: Silent Visual Text Query ("Thank you for watching") -> execute_single_case.py 3
     - Case 4: Spoken Query ("have you quite given up on me?") -> execute_single_case.py 4
     - Case 5: Multimodal Query ("At least tell me your name") -> execute_single_case.py 5
     - Case 6: Negative Control Query ("Batman") -> execute_single_case.py 6
     - Case 7: Unrelated Negative Control Query ("The blue elephant is dancing") -> execute_single_case.py 7

2. Video Source Links & Suite Runner:
   - Attach direct clickable YouTube links for all cases (https://www.youtube.com/watch?v=YVvD7SZ7kc0 and https://www.youtube.com/shorts/WZORRHNP9_w).
   - Feature python scripts/run_golden_suite.py as the single canonical benchmark suite runner.

============================================================
Prompt: 017
============================================================
Create a canonical benchmark command and preserve evidence artifacts:
1. Canonical Benchmark Runner:
   - Ensure scripts/run_golden_suite.py executes all 7 Golden Test Cases end-to-end and outputs a clean summary table showing PASS/FAIL status for each case.
   - Save individual evaluation JSON artifacts in tests/evaluation_results/ (case_01.json through case_07.json).
2. Evidence Preservation:
   - Preserve small intentional evidence JPG images in outputs/evaluation/ (case_01/ through case_05/).
   - Ensure large downloaded media files remain ignored in .gitignore to maintain clean repository hygiene.
3. Codebase Reverse-Engineering & Interview Defense:
   - Perform complete implementation-level reverse engineering across 21 structural parts (repository map, execution trace, configuration matrix, hyperparameters, formulas, temporal logic, visual search funnel, failure strategies, performance model, and recall/precision trade-offs).
   - Provide 20 realistic interview panel questions and technical defenses.

============================================================
Prompt: 018
============================================================
Create the Production Engineering Roadmap & Future Development Strategy:
1. Create docs/ENGINEERING_ROADMAP.md:
   - Document the v1.0.0 release as a deliberately scoped correctness baseline.
   - Define architectural triggers, limitations, requirements-driven evolution, and priority levels (P0 to P3).
   - Cover 5 core pillars: Retrieval Quality (semantic vector search, multilingual models), Evaluation & Reliability, Scale & Performance (persistent indexing, GPU acceleration), Product Capabilities (REST API, Web UI), and Enterprise Readiness.
   - Include configuration reference, tuning guide, decision matrix, and engineering restraint ("What We Would NOT Build Yet").
   - Provide an interview panel defense section detailing architectural choices.
2. README.md Integration:
   - Update README.md with an Engineering Roadmap section linking to docs/ENGINEERING_ROADMAP.md.

============================================================
Prompt: 019
============================================================
Incorporate the visual hierarchy architecture diagram into docs/ENGINEERING_ROADMAP.md:
- Update Section 3 of docs/ENGINEERING_ROADMAP.md with a top-level Mermaid flowchart depicting the v1.0.0 Baseline (Correctness, Evaluation, Core, ASR + OCR Retrieval) alongside the Evolution Roadmap (Quality, Scale, Product).
- Ensure all Mermaid diagrams are clean, valid, and GitHub-renderable.
- Run unit test suite and verify git status.

============================================================
Prompt: 020
============================================================
Update environment setup, single-query evaluation CLI instructions, and logging engine:
1. Environment Setup & Installation:
   - Provide fool-proof step-by-step instructions for git clone, virtual environment creation (`.venv`), activation (Linux/macOS vs Windows PowerShell), and `pip install -r requirements.txt`.
2. Logging Engine Overhaul:
   - Suppress third-party verbose HTTP/backend loggers (`httpx`, `httpcore`, `urllib3`, `onnxruntime`, `PIL`, `matplotlib`, `ppocr`, `yt_dlp`).
   - Format clean single-line video download output (`Downloaded video (9.05 MiB)`).
   - Add ANSI level color highlighting (`[INFO]`, `[SUCCESS]`, `[WARNING]`, `[ERROR]`) for transparent step-by-step terminal tracing.
3. Evaluation CLI Usage:
   - Detail CLI execution using python run.py --url "<URL>" --target "<QUERY>" for arbitrary public video evaluation.

============================================================
Prompt: 021
============================================================
Diagnose and resolve visual track truncation and multi-line headline banner recognition on news video:
1. News Video Edge Case Analysis:
   - Target Video: "Nepal Floods: Massive Flash Flood Wipes Out Nepal-China Border - Hundreds Feared Dead | WION Original" (YouTube ID Q8nTYEUNXlA, 26/08/2026).
   - Target Query: "Bihar on alert due to flood threat from Nepal".
   - Root Cause Diagnosis: Video generated 71 visual tracks. Default track limit (max_ocr_tracks = 20) truncated tracks naively ([:20]), cutting off late-appearing headlines. Multi-line banner (Yellow top line: "Bihar on alert due to flood threat", Black bottom line: "from Nepal") was evaluated as split boxes.
2. Architectural Resolutions:
   - Increase max_ocr_tracks default from 20 to 100.
   - Prioritize tracks by duration (track.end - track.start) and box count descending so persistent headlines are evaluated first.
   - Concatenate spatial text boxes into full_frame_text to evaluate multi-line text cards as unified phrases.
3. Documentation:
   - Document case study in docs/ARCHITECTURE.md and update engineering strategy in docs/ENGINEERING_ROADMAP.md.

============================================================
Prompt: 022
============================================================
Implement dynamic frame downscaling performance optimization and refine terminal logging formatting:
1. Dynamic Frame Downscaling Optimization:
   - Implement _load_scaled_image() helper in src/text_detector.py and src/ocr.py to resize high-resolution images (> 1024px) down to max dimension 1024px before passing to RapidOCR ONNX inference.
   - Scale detected bounding box coordinates back to original video frame dimensions to maintain 100% exact evidence frame extraction accuracy.
   - Achieve 4x–6x CPU speedup (reducing detection time from ~1.6s/frame to ~0.25s/frame).
2. Professional Log Formatting & Highlighting:
   - Enhance ColoredFormatter in run.py with ANSI level colors ([INFO] cyan, [SUCCESS] bold green, [WARNING] yellow, [ERROR] red).
   - Format stage dividers and diagnostic traces cleanly to prevent line wrapping clutter in terminal buffers without omitting any logs.
3. Verification:
   - Verify unit test suite (pytest tests/) and confirm clean CLI execution.

============================================================
Prompt: 023
============================================================
Document animated kinetic text evaluation case study and ASR-guided search window validation:
1. Animated Kinetic Text Case Study:
   - Target Video: "Batman: 'You've given them everything'" (YouTube Shorts URL: https://www.youtube.com/shorts/gmFEsT8CLNI).
   - Target Query: "you've given them everything".
   - Test Scenario: Video featuring kinetic/animated text subtitles appearing word-by-word on screen while spoken dialogue plays.
   - Validation 1 (ASR-Guided Search Windowing): ASR transcribes spoken dialogue at [10.02s, 11.38s] and restricts visual discovery to targeted window [8.52s, 14.88s], reducing OCR calls to 26 and total runtime to 56.77s.
   - Validation 2 (Kinetic Text Tracking): Multi-box IoU tracking tracks text building across frames (11.35s -> "YOU'VE GIVEN THEM EVERY", 11.69s -> "YOU'VE GIVEN THEM EVERYTHIN", 12.02s -> "YOU'VE GIVEN THEM EVERYTHING") and representative frame sampling isolates exact frame 12.020s with 1.0000 exact similarity score.
2. Documentation:
   - Document case study in docs/ARCHITECTURE.md and engineering roadmap.

